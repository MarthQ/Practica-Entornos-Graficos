---PHP: variables, tipos, operadores, expresiones, estructuras de control ---
1)
	Variables y sus tipos
		$a=boolean
		$b=string
		$c=string
		$d=integer
		$f=integer
		$g=integer

	Los operadores son
		++: unario
		+=:binario 
		*: binario


	Las funciones y sus parámetros
		function doble($i)
		gettype(variable)
		is_int(variable)
		is_string(variable)
		echo (variable, …)

	Las estructuras de control
		if
		$a? ++$d : $d*3

	Salidas por pantalla
		booleanstringstringinteger1xyzxyz184444

2)	
	a) Son equivalentes
	b) Son equivalentes
	c) Son equivalentes

3)	
	a) Se utiliza para crear una tabla a través de variables en una página html. Se define una varaible $row con el número de filas y una variable $col con el número de columnas.
	b)Se visualiza un campo donde, al ingresar un número entero se verifica si el usuario tiene 21 años o más.

4)
	a)Imprime la segunda sentencia pero la primera da error a causa que no tiene las variables porque el “include” aparece después.